# Development Environment Configuration
# This file contains development-specific settings that override production defaults

# Development mode
NODE_ENV=development

# Increased rate limits for development
RATE_LIMIT_WINDOW_MS=900000        # 15 minutes
RATE_LIMIT_MAX_REQUESTS=5000       # 5000 requests per window (very high for dev)
AUTH_RATE_LIMIT_MAX_REQUESTS=1000  # 1000 auth requests per window (very high for dev)

# Development CORS settings (more permissive)
ALLOWED_ORIGIN=http://localhost:3000,http://localhost:8080,http://localhost:5173,https://localhost:3000,https://localhost:8080,https://localhost:5173

# Development logging (more verbose)
LOG_LEVEL=debug

# Development trust proxy (for local testing)
TRUST_PROXY=false

# Note: In development, localhost requests will bypass rate limiting entirely
# This is configured in the server.js skip function
